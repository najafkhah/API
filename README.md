This project simulates API validation for integration between a Customer Relationship Management (CRM) platform and an external customer service system.
RESTful APIs were tested using Postman to validate CRUD operations, JSON payload structure, HTTP status codes, and error handling behaviour.
